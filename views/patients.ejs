<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <title>Patients Infos</title>
</head>
<body>
  <h1>Patients in <%= patients[0].id %></h1>
  <ul>
    <% patientss.forEach((patient) => { %>
      <li><%= patients.name %></li>
      <li><%= patients.Phone %></li>
      <li><%= patients.email %></li>
      <li><%= patients.date %></li>
      <li><%= patients.time %></li>
      <li><%= patients.message %></li>
    <% }) %>
  </ul>
  <a href="/">Back to top</a>

</body>
</html>
